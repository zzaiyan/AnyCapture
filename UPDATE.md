# AnyCapture 更新日志

## v0.1.4

### Bug修复
- 🛠️ **bfloat16兼容修复**：修复捕获变量在`.detach().cpu().numpy()`阶段可能报错的问题
  - 针对`bfloat16`等NumPy不支持的浮点类型，显式调用`.float()`转换为fp32后再写入缓存

### 新功能
- 🧩 **自定义collate_fn**：新增`set_collate_fn(collate_fn=None)`接口
  - 支持在缓存前注入自定义整理逻辑（如统计聚合、压缩、结构化转换）
  - 传入`None`可恢复内置默认整理策略

---

## v0.1.3

### 新功能
- ✨ **取消激活支持**：添加 `deactivate()` 方法，支持动态停用变量捕获
  - `get_local.deactivate()` - 停用装饰器，停止捕获新变量
  - `get_local.activate(max_size=N)` - 重新激活装饰器
  - `get_local.is_activated()` - 检查当前激活状态
- 🔄 **激活/停用循环**：支持反复切换激活状态，适应不同工作阶段
- 📊 **缓存保护**：停用时保留已有缓存数据，重新激活时继续工作

### 核心改进
- ⚡ **性能优化**：训练阶段可停用捕获以提高性能，调试时重新激活
- 🔧 **返回值一致性修复**：解决了停用状态下返回值格式不一致的问题
- 💾 **真正的原始函数保存**：使用 `types.FunctionType` 完整保存原始函数状态
- 🛡️ **状态管理**：完善的激活状态检查和管理机制

### 使用场景
- 🧠 **深度学习工作流**：训练时停用（提高性能）+ 推理时激活（调试分析）
- 🐛 **选择性调试**：仅在需要时启用变量捕获，其他时候保持最佳性能
- 🔄 **动态监控**：根据业务逻辑动态开启/关闭变量监控

---

## v0.1.2

### 新功能
- ✨ **队列机制**：添加缓存大小限制功能，支持队列模式
  - `activate(max_size=N)` - 设置队列最大容量
  - `set_size(N)` - 动态调整队列大小
  - 自动移除最早的元素，保持内存使用可控
- 🔧 **类型检查**：添加max_size参数的类型验证和警告机制
- 📝 **文档重构**：创建详细文档DOC.md，保持README简洁

### 改进
- 💪 **内存管理**：长时间运行时自动限制缓存增长
- ⚡ **性能优化**：动态队列调整，实时生效
- 🛡️ **错误处理**：非法类型输入时发出警告并设置为安全默认值

---

## v0.1.1

### 新功能
- ✨ **多变量捕获**：支持在单个装饰器中同时捕获多个局部变量
  - `@get_local('var1', 'var2', 'var3')` - 一次性捕获多个变量
  - 结构化缓存存储，便于批量分析

### 改进
- 📦 **缓存结构优化**：更好的组织多变量数据
- 🎯 **使用便利性**：减少装饰器数量，提高代码可读性

---

## v0.1.0

### 首次发布
- 🎉 **项目独立**：从[Visualizer](https://github.com/luo3300612/Visualizer)项目中独立
- 📦 **重新命名**：更名为AnyCapture以避免PyPI包名冲突
- 🚀 **PyPI发布**：发布到PyPI，支持pip安装
- 🔧 **核心功能**：
  - 局部变量捕获装饰器
  - 字典式缓存存储
  - 激活/停用机制
  - 缓存清理功能

### 技术特性
- 🎯 **精准捕获**：基于bytecode修改的变量捕获
- 🔄 **非侵入式**：不影响原函数执行逻辑
- 📊 **深度学习友好**：特别适用于Attention Map可视化

---

## 贡献者
- **原始作者**: [luo3300612](https://github.com/luo3300612) - Visualizer项目创建者
- **当前维护者**: [zzaiyan](https://github.com/zzaiyan) - AnyCapture项目维护者

## 特别感谢
感谢luo3300612创建的优秀Visualizer项目，为AnyCapture提供了坚实的技术基础。